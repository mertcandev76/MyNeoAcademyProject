@model List<ResultCourseEnrollmentDTO>

@{
    ViewData["Title"] = "Kurs Kayıtları";
}

<div class="container-fluid px-3">
    <div class="card shadow-sm">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="mb-0">@ViewData["Title"]</h3>
            <a asp-action="Create" asp-area="Instructor" class="btn btn-primary btn-sm">Ekle</a>
        </div>

        <div class="card-body p-0">
            @if (Model != null && Model.Any())
            {
                <table class="table table-bordered mb-0 text-center align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Kayıt ID</th>
                            <th>Kurs</th>
                            <th>Öğrenci</th>
                            <th>Kayıt Tarihi</th>
                            <th>İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var enrollment in Model)
                        {
                            <tr>
                                <td>@enrollment.Id</td>
                                <td>@enrollment.Course?.Title</td>
                                <td>@enrollment.AppUser?.FullName</td>
                                <td>@enrollment.EnrolledDate.ToString("dd/MM/yyyy")</td>
                                <td>
                                    <a asp-action="Detail" asp-area="Instructor" asp-route-id="@enrollment.Id" class="btn btn-primary btn-sm me-1">
                                        <i class="fas fa-eye"></i> Detay
                                    </a>
                                    <button type="button"
                                            class="btn btn-danger btn-sm"
                                            data-toggle="modal"
                                            data-target="#deleteModal"
                                            data-id="@enrollment.Id"
                                            data-name="@enrollment.Course?.Title"
                                            data-controller="CourseEnrollment"
                                            data-area="Instructor"
                                            title="Sil">
                                        <i class="fas fa-trash"></i> Sil
                                    </button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="alert alert-warning m-3 text-center">
                    Henüz kurs kaydı bulunmamaktadır.
                </div>
            }
        </div>
    </div>
</div>

<!-- Silme onay modalı -->
@await Html.PartialAsync("_DeleteModalPartial")
