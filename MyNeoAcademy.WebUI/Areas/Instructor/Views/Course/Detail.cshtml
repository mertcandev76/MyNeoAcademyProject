@model ResultCourseDTO

@{
    ViewData["Title"] = "Kurs Detayı";
}

<div class="container mt-5">
    <div class="card shadow-sm rounded border-primary">
        <div class="card-header">
            <h5 class="mb-0">@ViewData["Title"]</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">ID</dt>
                <dd class="col-sm-9">@Model.CourseID</dd>

                <dt class="col-sm-3">Başlık</dt>
                <dd class="col-sm-9">@Model.Title ?? "<span class='text-muted'>Yok</span>"</dd>

                <dt class="col-sm-3">Açıklama</dt>
                <dd class="col-sm-9">@Html.Raw(Model.Description?.Replace("\n", "<br />") ?? "<span class='text-muted'>Yok</span>")</dd>

                <dt class="col-sm-3">Kategori</dt>
                <dd class="col-sm-9">@Model.Category?.Name ?? "<span class='text-muted'>Yok</span>"</dd>

                <dt class="col-sm-3">Fiyat</dt>
                <dd class="col-sm-9">
                    @(Model.Price != null ? Model.Price.Value.ToString("C") : "<span class='text-muted'>Yok</span>")
                </dd>

                <dt class="col-sm-3">Eğitmen</dt>
                <dd class="col-sm-9">@Model.Instructor?.FullName ?? "<span class='text-muted'>Yok</span>"</dd>

                <dt class="col-sm-3">Resim</dt>
                <dd class="col-sm-9">
                    @if (!string.IsNullOrWhiteSpace(Model.ImageUrl))
                    {
                        <a href="@Model.ImageUrl" target="_blank">Resmi Görüntüle</a>
                    }
                    else
                    {
                        <span class="text-muted">Görsel bulunamadı.</span>
                    }
                </dd>

                <!-- Yeni eklenen alanlar -->
                <dt class="col-sm-3">Puan (Rating)</dt>
                <dd class="col-sm-9">@Model.Rating</dd>

                <dt class="col-sm-3">Yorum Sayısı (Review Count)</dt>
                <dd class="col-sm-9">@Model.ReviewCount</dd>

                <dt class="col-sm-3">Öğrenci Sayısı (Student Count)</dt>
                <dd class="col-sm-9">@Model.StudentCount</dd>

                <dt class="col-sm-3">Beğeni Sayısı (Like Count)</dt>
                <dd class="col-sm-9">@Model.LikeCount</dd>

            </dl>

            <div class="d-flex justify-content-between">
                <a asp-action="MyCourses" class="btn btn-secondary">Geri Dön</a>
                <a asp-action="Edit" asp-route-id="@Model.CourseID" class="btn btn-warning">Düzenle</a>
            </div>
        </div>
    </div>
</div>
